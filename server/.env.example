# Familyhub OS - Environment Configuration
# Copy this file to .env and fill in your values

# Google OAuth credentials (for calendar integration)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# Google OAuth redirect URI (optional, defaults to http://localhost:3001/api/google/callback)
# Set this to your actual URL for production deployments
# GOOGLE_REDIRECT_URI=http://your-pi-ip:3001/api/google/callback

# Server configuration
PORT=3001
HOST=0.0.0.0

# Environment (production enables JSON logging, stricter validation)
# NODE_ENV=production

# CORS - Allowed origins for API requests (comma-separated)
# The install script auto-generates this with familyhub.local and your Pi's IP
# Example: ALLOWED_ORIGINS=http://familyhub.local,http://192.168.1.100

# Encryption key for sensitive data (Paprika credentials, etc.)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_character_hex_key_here
