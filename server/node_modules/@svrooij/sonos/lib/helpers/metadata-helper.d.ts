import { Track } from '../models/track';
export default class MetadataHelper {
    private static debug;
    /**
     * ParseDIDLTrack will parse track metadata for you.
     *
     * @static
     * @param {*} didl Object from XmlParser
     * @param {string} host Sonos host, to make album uri an absolute url
     * @param {number} [port=1400] Sonos port, to make album uri an absolute url
     * @returns {Track} Parsed track
     * @memberof MetadataHelper
     */
    static ParseDIDLTrack(didl: unknown, host: string, port?: number): Track | undefined;
    /**
     * Track to MetaData will generate a XML string that can be used as MetaData
     *
     * @static
     * @param {Track} track The track do describe
     * @returns {string} XML string (be sure to encode before using)
     * @memberof MetadataHelper
     */
    static TrackToMetaData(track: Track | undefined, includeResource?: boolean, cdudn?: string): string;
    static GuessMetaDataAndTrackUri(trackUri: string, spotifyRegion?: string): {
        trackUri: string;
        metadata: Track | string;
    };
    static GuessTrack(trackUri: string, spotifyRegion?: string): Track | undefined;
    private static guessSpotifyMetadata;
    private static deezerMetadata;
    private static appleMetadata;
    private static GetUpnpClass;
}
//# sourceMappingURL=metadata-helper.d.ts.map