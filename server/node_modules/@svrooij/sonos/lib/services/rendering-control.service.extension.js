"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RenderingControlService = void 0;
const rendering_control_service_1 = require("./rendering-control.service");
class RenderingControlService extends rendering_control_service_1.RenderingControlServiceBase {
    ResolveEventPropertyValue(name, originalValue, type) {
        if (name === 'Mute') {
            const output = {};
            originalValue.forEach((v) => {
                output[v.channel] = v.val === '1';
            });
            return output;
        }
        if (name === 'Volume') {
            const output = {};
            originalValue.forEach((v) => {
                output[v.channel] = parseInt(v.val, 10);
            });
            return output;
        }
        return super.ResolveEventPropertyValue(name, originalValue, type);
    }
}
exports.RenderingControlService = RenderingControlService;
//# sourceMappingURL=rendering-control.service.extension.js.map