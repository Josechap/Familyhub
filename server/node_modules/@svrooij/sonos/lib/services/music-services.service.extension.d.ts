import { MusicServicesServiceBase } from './music-services.service';
export interface MusicService {
    Capabilities: string;
    ContainerType: string;
    Id: number;
    Manifest: {
        Uri: string;
        Version: string;
    };
    Name: string;
    Policy: {
        Auth: 'Anonymous' | 'AppLink' | 'DeviceLink' | 'UserId';
        PollInterval?: number;
    };
    Presentation?: {
        Strings?: {
            Uri: string;
            Version: string;
        };
        PresentationMap?: {
            Uri: string;
            Version: string;
        };
    };
    SecureUri: string;
    Uri: string;
    Version: string;
}
export declare class MusicServicesService extends MusicServicesServiceBase {
    private musicServices?;
    /**
     * A parsed version of ListAvailableServices
     *
     * @param {boolean} [cache=false] Should the list be fetched once and then kept in memory?
     * @returns {Promise<Array<MusicService>>} All available music services (not only subscribed ones).
     * @memberof MusicServicesService
     */
    ListAndParseAvailableServices(cache?: boolean): Promise<Array<MusicService>>;
    private static ParseMusicService;
}
//# sourceMappingURL=music-services.service.extension.d.ts.map