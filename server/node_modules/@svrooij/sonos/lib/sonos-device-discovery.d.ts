export default class SonosDeviceDiscovery {
    private readonly debug;
    private readonly devices;
    private readonly socket;
    private readonly events;
    private pollTimeout;
    private searchTimeout;
    private isBound;
    private readonly DeviceAvailable;
    get port(): number | undefined;
    constructor();
    private readonly SEARCH_STRING;
    private sendBroadcast;
    private sendBroadcastToAddress;
    private setCancelTimeout;
    SearchOne(timeoutSeconds?: number): Promise<Player>;
    Search(timeoutSeconds?: number): Promise<Player[]>;
    private Cancel;
}
interface Player {
    host: string;
    port: number;
    model: string | undefined;
}
export {};
//# sourceMappingURL=sonos-device-discovery.d.ts.map